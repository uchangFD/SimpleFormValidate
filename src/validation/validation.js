class Validation {
  validationNodes = [];

  createNode() {}
  removeNode(name) {
    // return false;
  }
  updateNode(validationInfo) {
    // return false;
  }
  setMatcher(name, isAsync) {}
  getNode(name) {
    // return validationNode
  }
}

export default Validation;
